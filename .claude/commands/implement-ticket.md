Implement the following Dialuna React Native ticket:

$ARGUMENTS

Required process:

1. Read `CLAUDE.md`, `package.json`, and `app.json`.
2. Inspect `git status --short` and do not overwrite existing user changes.
3. Locate relevant routes, screens, components, stores, services, types, translations, tests, and Expo config.
4. Explain the current behavior and likely root cause.
5. Propose a concise implementation plan and list expected files to change.
6. Implement the smallest safe change.
7. Add or update focused tests where appropriate.
8. Run relevant validation, starting with targeted tests or typecheck.
9. Review `git diff --check` and the final diff.
10. Return:
    - root cause
    - changed files
    - implementation summary
    - validation results
    - localization notes
    - iOS/Android/web considerations
    - remaining risks

Do not commit or push.
