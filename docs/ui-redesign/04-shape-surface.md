# Shape and surface

Radius scale: 8, 12, 18, 24, 32, 40, and capsule. Not every section is a card and not every action is a pill.

Surface hierarchy is canvas → embedded → raised → floating → modal. Only floating/modal context may use blur. A single component must never combine heavy blur, bright border, large shadow, gradient, and glow.
